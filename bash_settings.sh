#!/bin/bash

# Some commons
export GIT_EDITOR=vim
export RAILS_ENV=development_emobile
alias ack='ack -ri'
alias grep='grep -ri'

# Load RVM
[[ -s "$HOME/.rvm/scripts/rvm" ]] && source "$HOME/.rvm/scripts/rvm"
